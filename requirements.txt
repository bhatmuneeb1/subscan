# Subscan 0.2.0 has no required third-party Python packages.
#
# The reconnaissance engines it orchestrates (subfinder, amass, httpx,
# assetfinder, findomain, httprobe, subjack, and subzy) are external command-line
# tools and should be installed from their respective upstream projects.
#
# Install Subscan itself with:
#   python -m pip install -e .
